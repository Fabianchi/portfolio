git init

git config --global user.name 'Your Name'
git config --global user.email you@somedomain.com

git remote add origin https://github.com/Fabianchi/portfolio.git

git clone https://github.com/Fabianchi/portfolio.git

git add *

//git status muestra el conjunto de cambios realizados y agregados
//git status -s muestra una salida corta
//git diff muesta los cambios en los archivos
//git diff --stat

git commit -m 'my hola mundo changes'

git push origin master
